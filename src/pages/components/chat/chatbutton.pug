mixin chatbutton(data)
    .chatbutton
        .image(style="background-image: url('" + data.image + "');")
        .chatinfo
            p.name= data.name
            p.message= data.message
        .status
            p.date= data.date
            p.count= data.count
        